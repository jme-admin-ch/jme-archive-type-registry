@namespace("ch.admin.bit.jeap.processarchive.test.decreesummary.v1")
protocol DecreeSummaryProtocol {

  record DecreeSummary {
    string decreeId;
    timestamp_ms createdAt;
    string title;
  }
}
